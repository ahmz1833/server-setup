# Managed by Ansible

Port {{ sandbox_ssh_port }}

Match LocalPort {{ core_ssh_port | default('22') }}
    DenyUsers {{ sandbox_shared_user }}

Match User {{ sandbox_shared_user }}
    AllowTcpForwarding no
    X11Forwarding no
    PermitTunnel no
    AllowAgentForwarding no
