---
galaxy_info:
  role_name: apps
  author: ahmz
  description: >-
    Docker container lifecycle manager — creates directories, ensures
    networks, reconciles container state, and prunes unmanaged containers
    with configurable whitelist.
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions:
        - focal
        - jammy
        - noble
    - name: Debian
      versions:
        - bullseye
        - bookworm
  galaxy_tags:
    - docker
    - containers
    - deployment
    - apps

dependencies: []
