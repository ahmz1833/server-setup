# Managed by Ansible
# Resolved {{ nginx_resolved_trusted_proxies | length }} trusted proxy CIDRs

{% for proxy in nginx_resolved_trusted_proxies %}
set_real_ip_from {{ proxy }};
{% endfor %}

real_ip_header {{ nginx_real_ip_header }};
real_ip_recursive on;
