server {
    listen 127.0.0.1:{{ nginx_dummy_metric_port }};
    server_name 127.0.0.1;

    location /metrics {
        stub_status on;
        access_log off;
        allow 127.0.0.1;
        deny all;
    }

    # Blackhole for Promtail when Loki is not configured
    location /loki/api/v1/push {
        access_log off;
        return 204;
    }
}
