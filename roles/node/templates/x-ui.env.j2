# {{ ansible_managed }}
# x-ui service environment file.
# Loaded by systemd (EnvironmentFile=) and by the `x-ui` CLI (migrate/setting),
# so the panel and the CLI always agree on the database backend and options.
{% for key, value in _node_xui_env_all | dictsort %}
{{ key }}={{ value }}
{% endfor %}
